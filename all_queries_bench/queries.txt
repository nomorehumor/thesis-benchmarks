SELECT name FROM person WHERE city == VARSIZED("redmond") INTO NAME_SINK
SELECT extra FROM person WHERE name == VARSIZED("kate jones") INTO EXTRA_SINK
SELECT * FROM person WHERE city == VARSIZED("redmond") INTO PERSON_SINK 
SELECT id FROM person WHERE city == VARSIZED("redmond") INTO PERSON_ID_SINK
SELECT name FROM person WHERE email_address == VARSIZED("dsiqwyi@hkrxb.com") INTO NAME_SINK

SELECT auctionId FROM bid WHERE bidder == INT32(1001) INTO BID_AUCTION_ID_SINK
SELECT auctionId, price FROM bid WHERE auctionId % INT32(123) = INT32(0) INTO ID_PRICE_AUCTION_SINK
SELECT auctionId FROM bid WHERE price = FLOAT64(220.0) INTO BID_AUCTION_ID_SINK

SELECT id FROM person WHERE CONCAT(city, state) == VARSIZED("redmondwy") INTO PERSON_ID_SINK
SELECT id FROM person WHERE city == VARSIZED("redmond") AND state == VARSIZED("wy") INTO PERSON_ID_SINK

SELECT extra FROM person INTO EXTRA_SINK
SELECT CREDIT_CARD FROM PERSON INTO CREDIT_CARD_SINK
